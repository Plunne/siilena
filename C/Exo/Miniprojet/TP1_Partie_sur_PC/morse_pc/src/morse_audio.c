#include "morse_audio.h"

void beepAudio(int t) {

    // Function that beeps a sound of
    // frequency 440 Hertz for 0.8 sec
    // t : timerX_ms * 10 (because timers are centi-seconds)
    //Beep(440, t * 10);
    
}
