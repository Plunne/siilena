#include "pendufile.h"

char* getFileWord(PenduGame* Pendu) {

	/*
	Pendu->file = fopen("PenduList.txt", "r");

	if (!Pendu->file) exit(EXIT_FAILURE);

	int file_size = 0;
	
	while (fgets(Pendu->file) != NULL) file_size++;

	printf("%d", file_size);

	fclose(Pendu->file);

	return "file";
	*/
}
